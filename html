<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Navintran Supermart</title>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
<style>
  :root {
    --primary: #006633; /* Malaysian flag green */
    --secondary: #FFCC00; /* Malaysian flag yellow */
    --dark: #333;
    --light: #f5f5f5;
  }

  body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;git
    background-color: var(--light);
  }

  .header {
    background-color: var(--primary);
    color: var(--secondary);
    padding: 20px;
    text-align: center;
  }

  .header h1 {
    margin: 0;
    font-size: 2em;
  }

  .navbar {
    display: flex;
    justify-content: center;
    background-color: var(--dark);
    padding: 10px;
  }

  .navbar a {
    color: white;
    padding: 14px 20px;
    text-decoration: none;
    text-align: center;
  }

  .navbar a:hover {
    background-color: var(--secondary);
    color: var(--dark);
  }

  .container {
    padding: 20px;
  }

  .product {
    border: 1px solid #ddd;
    border-radius: 5px;
    margin-bottom: 20px;
    padding: 15px;
    background-color: white;
  }

  .product h3 {
    margin-top: 0;
  }

  .product button {
    background-color: var(--primary);
    color: white;
    border: none;
    padding: 10px 15px;
    cursor: pointer;
    border-radius: 5px;
  }

  .product button:hover {
    background-color: var(--secondary);
    color: var(--dark);
  }

  .cart {
    margin-top: 40px;
  }

  .cart h2 {
    text-align: center;
  }

  .cart-item {
    background-color: white;
    border: 1px solid #ddd;
    border-radius: 5px;
    margin-bottom: 10px;
    padding: 10px;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .cart-item button {
    background-color: var(--danger);
    color: white;
    border: none;
    padding: 5px 10px;
    cursor: pointer;
    border-radius: 5px;
  }

  .cart-item button:hover {
    background-color: var(--dark);
  }
</style>
</head>
<body>

<div class="header">
  <h1>Navintran Supermart</h1>
</div>

<div class="navbar">
  <a href="#">Home</a>
  <a href="#">Products</a>
  <a href="#">About</a>
  <a href="#">Contact</a>
</div>

<div class="container">
  <div class="product" data-id="1">
    <h3>Product 1</h3>
    <p>Price: $10</p>
    <button onclick="addToCart(1)">Add to Cart</button>
  </div>
  <div class="product" data-id="2">
    <h3>Product 2</h3>
    <p>Price: $15</p>
    <button onclick="addToCart(2)">Add to Cart</button>
  </div>
  <!-- Add more products as needed -->
</div>

<div class="cart">
  <h2>Shopping Cart</h2>
  <div id="cartItems"></div>
</div>

<script>
  const cart = {};

  function addToCart(productId) {
    if (cart[productId]) {
      cart[productId].quantity++;
    } else {
      cart[productId] = { quantity: 1 };
    }
    updateCart();
  }

  function updateCart() {
    const cartItemsContainer = document.getElementById('cartItems');
    cartItemsContainer.innerHTML = ''; // Clear current cart items

    for (const productId in cart) {
      const cartItem = document.createElement('div');
      cartItem.className = 'cart-item';
      cartItem.innerHTML = `
        <span>Product ${productId} - Quantity: ${cart[productId].quantity}</span>
        <button onclick="removeFromCart(${productId})">Remove</button>
      `;
      cartItemsContainer.appendChild(cartItem);
    }
  }

  function removeFromCart(productId) {
    if (cart[productId]) {
      cart[productId].quantity--;
      if (cart[productId].quantity === 0) {
        delete cart[productId];
      }
      updateCart();
    }
  }
</script>

</body>
</html>
